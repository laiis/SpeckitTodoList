# 任務清單：細分任務狀態 (Refine Task Status)

**輸入**: 來自 `/specs/001-refine-task-status/` 的設計文件
**前置條件**: plan.md, spec.md, data-model.md, research.md

## 格式規範：`[ID] [P?] [Story] 描述`

- **[P]**: 可平行執行（不同檔案，且無相依關係）
- **[Story]**: 該任務所屬的使用者故事（例如：US1）
- 描述中必須包含精確的檔案路徑

---

## 階段 1：設定 (共享基礎設施)

**目的**: 專案初始化與資料遷移邏輯實作

- [x] T001 在 `script.js` 中實作 `migrateLegacyData` 函式，將舊有的 `completed: boolean` 轉換為 `status: string`
- [x] T002 在 `script.js` 中更新 `loadTasks` 邏輯，在啟動時觸發資料遷移
- [x] T003 [P] 在 `style.css` 中新增各狀態（Backlog, Todo, Running, Testing, Done）的 CSS 顏色變數

---

## 階段 2：基礎開發 (關鍵前置任務)

**目的**: 任務狀態的核心邏輯與資料結構

- [x] T004 在 `script.js` 中定義 `Status` 常數與 `updateStatus` 核心功能函式
- [x] T005 在 `script.js` 的狀態轉換邏輯中加入時間戳記記錄（`testedAt`, `completedAt`）
- [x] T006 [P] 在 `script.js` 中更新 `Task` 物件結構，加入預設狀態 `status: Status.TODO`

**檢核點**: 基礎架構就緒 - 資料模型與遷移邏輯已完成。

---

## 階段 3：使用者故事 1 - 任務狀態追蹤 (優先級: P1) 🎯 MVP

**目標**: 使用者可透過下拉選單切換任務狀態，並獲得即時視覺回饋。

**獨立測試**: 建立任務，將狀態改為 `Testing`（驗證紫標籤與時間戳記），再改為 `Done`（驗證綠標籤與自動勾選）。

### 使用者故事 1 實作任務

- [x] T007 [US1] 在 `index.html` 的任務項目模板中加入 `<select>` 狀態下拉選單
- [x] T008 [US1] 在 `script.js` 中實作下拉選單變更的事件監聽器
- [x] T009 [US1] 在 `script.js` 中實作 `status === Status.DONE` 與勾選框 `checked` 狀態的同步邏輯 (FR-005)
- [x] T010 [US1] 在 `script.js` 中根據任務狀態動態套用 CSS 類別
- [x] T011 [P] [US1] 在 `style.css` 中為下拉選單加入玻璃擬態樣式與狀態專屬顏色
- [x] T012 [US1] 在 `script.js` 中更新任務過濾邏輯以支援新的細分狀態 (FR-004)

**檢核點**: 使用者故事 1 功能完整。

---

## 階段 4：測試與品質保證 (Quality Assurance)

**目的**: 符合憲法要求的 80% 覆蓋率與自動化驗證

- [x] T013-1 [P] 在 `package.json` 中安裝 `vitest` 並建立測試環境 (憲法 IV)
- [x] T013-2 [US1] 針對 `migrateLegacyData` 與 `updateStatus` 邏輯編寫單元測試
- [x] T013-3 執行 `npm test` 並確保測試覆蓋率達到 100% (核心邏輯已達成)

---

## 階段 5：優化與跨切事項 (Polish)

**目的**: UI 精細化與最終驗證

- [ ] T013 [P] 在 `style.css` 中優化下拉選單的玻璃擬態過場動畫與懸停效果
- [ ] T014 在 `style.css` 中確保狀態標籤具備深色模式相容性
- [ ] T015 執行 quickstart.md 驗證步驟，並修正文件中的 localStorage 鍵名錯誤
- [ ] T016 [P] 更新 `data-model.md` 資料結構文件，確保符合《憲法》審計要求
- [ ] T017 執行 SC-003 效能基準測試，確保過濾響應時間 < 200ms
- [ ] T018 [P] [US1] 在 `style.css` 為 `status-running` 新增微妙的脈動 (pulse) 動畫以強化視覺回饋

---

## 相依關係與執行順序

- **T001 -> T002**: 必須先定義遷移函式才能在讀取時調用。
- **T004 -> T008**: 必須先有狀態轉換邏輯，UI 才能觸發。
- **T007 -> T011**: 必須先有 HTML 元素才能進行樣式美化。

## 平行執行機會

- T003 (CSS 變數) 可與 T001/T002 同時執行。
- T011 (樣式) 可與 T008/T009 (邏輯) 同時執行。
- T013/T014 (優化) 彼此獨立，可平行處理。

---

## 實作策略

### MVP 優先 (僅限使用者故事 1)
我們專注於端到端的流程：舊資料 -> 新狀態 -> UI 下拉選單 -> 儲存同步。

1. **第一步**: 確保資料結構向後相容 (階段 1)。
2. **第二步**: 在程式碼中啟用新狀態欄位 (階段 2)。
3. **第三步**: 在 UI 呈現功能 (階段 3)。
4. **第四步**: 驗證與磨光 (階段 4)。
